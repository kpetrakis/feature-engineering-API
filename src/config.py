import os
cwd = os.getcwd()
data_path = cwd + "/data/cvas_data.json"

features_dir = cwd + "/features"
features_customers_path = cwd + "/features/features_customers.json"
features_loans_path = cwd + "/features/features_loans.json"


